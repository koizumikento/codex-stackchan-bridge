interface:
  display_name: "Codex StackChan"
  short_description: "Signal Codex work state and interpret StackChan observations"
  default_prompt: "Use $codex-stackchan to send restrained StackChan work-state cues or interpret local StackChan observations through stackchanctl."
policy:
  allow_implicit_invocation: true
