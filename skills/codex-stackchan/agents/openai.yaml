interface:
  display_name: "Codex StackChan"
  short_description: "Signal Codex work state through StackChan"
  default_prompt: "Use $codex-stackchan to signal Codex work progress through the local StackChan."
policy:
  allow_implicit_invocation: true
